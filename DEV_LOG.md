# TERRITORY GAME 개발 일지

## 2026-09-05 — v0.3 Territory 기반 21영역 / 3팀 전환

### 방향 전환
기존의 작은 원형 거점 + 연결선 게임에서, 실제로 넓은 땅을 먹는 느낌이 나는 Territory 기반 땅따먹기로 구조를 변경했다.

### 핵심 결정
- 실제 점령 단위는 동그라미가 아니라 Territory 영역 전체
- 동그라미는 각 Territory의 현재 병력 숫자 표시용 UI
- 총 21개 Territory
- 3팀 구성: 파랑(플레이어) / 빨강 AI / 노랑 AI
- 시작 Territory는 팀별 2개
- 나머지는 중립 Territory
- Territory 크기를 서로 다르게 구성
- Territory 크기에 따라 생산력 0.9~1.4 범위 적용
- 큰 땅이 유리하지만 2배 생산처럼 과도한 스노우볼은 방지

### 조작
- 내 Territory에서 드래그 시작
- 드래그 중 지나간 파란 Territory를 여러 개 묶어서 선택
- 적/중립 목표 Territory에서 손을 떼면 선택한 Territory 병력이 동시에 출병
- 병력은 현재 병력의 50% 출병
- 뒤쪽 Territory 병력은 목표와 직접 붙어 있지 않아도 아군 Territory를 자동 경유
- 경로는 같은 팀 Territory를 통과하고 마지막 한 칸만 목표 Territory로 진입

### 맵 / 전투
- 빈틈 없이 맞물리는 7×3 불규칙 Territory 맵
- Territory 전체가 팀 색상으로 표시
- 선택 Territory는 밝은 테두리
- 목표 Territory는 흰색 테두리
- 파랑/빨강/노랑 모두 병력 자동 생산
- 빨강/노랑 AI가 주변 중립/적 Territory를 자동 공격
- 병력 이동 애니메이션 유지
- 단순 병력 수 기반 전투/점령 유지

### 보류
- 좌우 협공, 포위, 전선 압박 등 2D 전쟁 시뮬레이션 요소는 별도 게임/별도 스타트 아이디어로 분리
- 현재 프로젝트는 순수 Territory 땅따먹기에 집중

---

## 2026-09-05 — v0.2.4 연속 점령 입력 수정

### 수정 목적
첫 Territory 점령 후 새로 먹은 Territory에서 다음 Territory로 이어서 드래그할 때 선택으로 오인되는 문제를 줄인다.

### 완료
- 손을 뗀 위치가 시작 거점이면 선택
- 다른 거점에서 손을 떼면 이동/공격 시도
- 새로 점령한 거점에서도 즉시 다음 출병 가능하도록 입력 흐름 정리
- 소유권 변경 시 이전 선택 상태 제거

---

## 2026-09-05 — v0.2.3 선택/출병 분리 실험

### 수정 목적
탭 선택과 드래그 출병이 서로 간섭하던 문제를 분리한다.

### 완료
- 탭은 선택 상태 변경
- 드래그는 출병만 수행
- 드래그 중 선택 상태를 바꾸지 않도록 처리
- 선택된 여러 거점에서 하나의 목표로 동시 출병 가능

---

## 2026-09-05 — v0.2.2 선택/이동 독립화

### 수정 목적
범위 선택과 이동이 같은 제스처에서 충돌하는 문제를 개선한다.

### 완료
- 선택 동작과 이동 동작을 분리하는 구조로 변경
- 이미 선택된 여러 거점에서 목표로 동시 출병 가능
- 직접 연결된 거점만 출병하는 기존 경로 제한은 유지

---

## 2026-09-05 — v0.2.1 탭 복구 + 인근 판정 범위 확대

### 수정 목적
v0.2 드래그 다중 선택을 넣은 뒤 일반 탭 선택이 제대로 되지 않는 문제를 수정하고, 거점 중심을 정확히 누르지 않아도 주변 범위에서 선택/목표 지정이 가능하도록 입력 판정을 넓힌다.

### 완료
- 일반 탭으로 내 거점 선택 기능 복구
- 선택된 거점에서 적/중립 거점 탭 공격 복구
- 드래그 판정 기준을 SVG 좌표가 아니라 실제 화면 픽셀 이동량 기준으로 변경
- 손가락이 조금 흔들려도 탭으로 인식되도록 12px 이동 임계값 적용
- 거점 실제 원 반경보다 넓은 약 88 SVG 단위의 히트 영역 추가
- 거점 중심을 직접 누르지 않아도 인근에서 선택 가능
- 드래그 중 거점 인근에 들어오면 자동으로 해당 거점을 감지
- 인근 감지 시 점선 범위 표시
- 적/중립 거점 주변에서 손을 떼도 목표로 지정 가능
- 여러 거점 드래그 선택 기능 유지
- 기존 AI/전투/점령 로직 유지

### 현재 조작
- 짧게 탭: 내 거점 선택 / 목표 공격
- 파란 거점에서 길게 이동: 드래그 다중 선택
- 드래그 중 다른 파란 거점 인근 통과: 선택 추가
- 적/중립 거점 인근에서 손 떼기: 선택 거점들에서 동시 출병

---

## 2026-09-05 — v0.2 드래그 다중 선택

### 작업 목적
단일 거점 탭 방식보다 광고에서 보던 조작감에 가깝게, 손가락 드래그 한 번으로 여러 내 거점을 선택하고 한 목표에 동시에 병력을 보내는 기능을 추가한다.

### 완료
- 파란 거점에서 드래그 시작
- 드래그 중 지나간 파란 거점 다중 선택
- 선택 거점 노란 테두리 표시
- 목표 거점 위에서 손을 떼면 동시 출병
- 목표와 직접 연결된 선택 거점만 실제 출병
- 드래그 중 목표와 연결 가능한 경로 강조
- 기존 탭 선택 → 출병 조작 유지
- Pointer Events 기반 모바일/마우스 공통 처리
- 기존 AI/전투/점령/승패 로직 유지
- 버전 표시 v0.2로 갱신

### 현재 제한
- 여러 선택 거점이 하나의 동일 목표로만 출병
- 선택한 거점과 목표 사이에 직접 연결선이 있어야 출병 가능
- 출병량 50% 고정
- 드래그 경로를 따라 여러 목표를 연속 지정하는 기능은 아직 없음

---

## 2026-09-05 — v0.1 최초 프로토타입

### 작업 목적
광고형 땅따먹기 게임의 핵심 플레이만 분리하여, 실제로 바로 플레이 가능한 단일 HTML 프로토타입을 만든다.

### 완료
- 프로젝트를 STARBUCKS HELPER와 완전 분리
- GitHub 저장소 `runipokr-dotcom/Game`에 별도 프로젝트 구성
- `index.html` 기반 프로토타입 생성
- 플레이어/적/중립 거점 구현
- 거점 병력 자동 증가 구현
- 내 거점 선택 → 다른 거점 터치 출병 구현
- 현재 병력의 절반 출병 구현
- 이동 병력 표시 구현
- 전투/점령 계산 구현
- 간단한 AI 자동 공격 구현
- 승리/패배 판정 구현
- 모바일 터치 대응
- 재시작 버튼 구현
- 광고/로그인/인앱결제 요소 없음

### 버전 메모
`v0.1`은 "재미가 있는지 확인하는 최소 플레이 가능 버전"이다.