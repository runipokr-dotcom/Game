# TERRITORY GAME 개발 일지

## 2026-09-05 — v0.1.8 맵 축소 / 난이도 완화

### 맵 조정
- v0.1.7의 40 Territory 대형 전장이 실제 플레이에서 과하게 복잡하고 난도가 높아 맵을 한 단계 축소.
- 총 Territory를 **40 → 30개**로 조정.
- 전장 좌표계를 `2200 × 1320 → 1650 × 1050`으로 축소.
- 작은 Territory 다수 + 중대형 Territory 일부 분포는 유지.
- 랜덤 시작과 중앙/내부 시작 가능 규칙 유지.

### 난이도
- 승리조건은 전체의 60% 원칙을 유지해 **18 / 30**으로 변경.
- AI 판단 주기를 `2.0초 → 2.4초`로 완화해 초반 확장 여유를 조금 늘림.
- 병력 이동속도는 `3.2 이동 타일/초` 그대로 유지.

### 유지
- 실제 병력 이동, 숨은 생산/이동 타일, 독립 병력군, 도착순 전투, 연쇄 출병, 아군 보충, 탭 최단경로, 화면 이동/줌 유지.

---

## 2026-09-05 — v0.1.7 대형 전장 / 40 Territory / 랜덤 시작

### 대형 맵 전환
- Territory를 21개에서 **40개**로 확대.
- 전장 내부 좌표계를 `2200 × 1320`으로 확대해 장거리 이동과 전선 형성 공간을 확보.
- 작은 Territory를 다수 배치하고 중형/대형 Territory를 일부 섞어 경제적 가치 차이를 강화.
- 빈 배경을 드래그하면 전장을 이동할 수 있고 `+ / -` 버튼 및 마우스 휠로 확대/축소 가능.

### 랜덤 시작
- 파랑/빨강/초록의 시작 Territory 2개를 매 새 게임마다 랜덤 배정.
- 시작 2개는 서로 인접한 Territory로 구성.
- 세 팀 시작 지점은 가능한 한 서로 멀리 떨어진 조합을 선택.
- 맵 꼭짓점 고정 규칙 제거. 중앙/측면/내부 어디서든 시작 가능.
- 각 팀 첫 본진은 주변 Territory보다 크게 설정. 플레이어 본진은 생산 타일 26개(+2.6/s), AI 본진은 22개(+2.2/s).

### 영토 크기 / 생산
- 기존 숨은 생산 타일 규칙 유지: 생산 타일 1개 = `+0.1 병력/초`.
- 일반 Territory는 소형 다수, 중대형 소수 분포.
- Territory 표시 면적과 생산량은 같은 tileCount를 사용.

### 이동
- 숨은 이동 타일 체계 유지.
- 병력 이동속도를 `2.6 → 3.2 이동 타일/초`로 소폭 상향.
- 독립 병력군, 경로 고정, 실제 도착 순서 전투 규칙 유지.

### 승리
- 대형 맵에 맞춰 기존 13/21 고정 조건을 제거.
- 전체 40개 중 **24개(60%) 확보 시 즉시 승리**.

---

## 2026-09-05 — v0.1.6 숨은 이동 타일 기반 동선 계산

- 생산 타일과 이동 타일을 별도 숨은 레이어로 분리.
- 이동 타일 1칸 약 55px, 기존 이동속도 2.6칸/초.
- 다익스트라 최단경로와 도착시간을 이동 타일 비용으로 통일.
- 자동/수동 경로 모두 A→B→C→D 연쇄 출병 시 A/B/C가 독립 출병하고 실제 도착 순서대로 전투.

---

## 2026-09-05 — v0.1.5 내 영토 최단경로 + 거점 간격 확대
- 탭 2회 자동 최단경로, 수동 드래그 경로 공존.

## 2026-09-05 — v0.1.4 마지막 참고 이미지형 전장 재설계
- 마지막 참고 이미지의 넓은 전장/거점 간 거리 가독성을 1순위 방향으로 채택.

## 2026-09-05 — v0.1.3 아군 보충 / 맵 가독성 수정
- 아군 Territory 보충도 실제 이동 객체로 처리.

## 2026-09-05 — v0.1.2 보이지 않는 타일 기반 Territory / 드래그 경로 이동
- 숨은 생산 타일 1개 = +0.1/s. 드래그 경로의 각 아군 Territory가 독립 출병.

## 2026-09-05 — v0.1 기준 재정립 / 이동 병력 프로토타입 재구축
- 핵심을 `실제 이동 병력 + 경로/거리 + 순차 도착 전투`로 재고정.

---

## 과거 실험 기록
이전 v0.2~v0.3 실험은 현재 공식 기준이 아니며, v0.1 계열의 실제 이동/영역전 구조를 계속 발전시킨다.