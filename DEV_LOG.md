# TERRITORY GAME 개발 일지

## 2026-09-05 — v0.1.2 보이지 않는 타일 기반 Territory / 드래그 경로 이동

### 핵심 구조 수정
- 각 Territory 내부를 플레이어에게 보이지 않는 작은 타일 집합으로 구성한다.
- 기본 타일 1개 생산량은 `0.1 / 초`.
- Territory 생산량은 `보유 타일 수 × 0.1 / 초`로 계산한다.
- 예: 8타일 Territory = `0.8 / 초`, 12타일 Territory = `1.2 / 초`.
- 따라서 Territory의 실제 크기와 생산량이 같은 기준에서 함께 결정된다.
- 내부 타일 경계는 화면에 표시하지 않는다. 플레이어에게는 하나의 연속된 Territory로만 보인다.
- 전체 맵 외곽도 직사각형이 아니라 불규칙한 육지 실루엣으로 만든다.

### 드래그 출병
- 파란 Territory A에서 드래그를 시작하고 B → C를 순서대로 통과한 뒤 D에 드롭한다.
- A 병력 경로: A → B → C → D
- B 병력 경로: B → C → D
- C 병력 경로: C → D
- 각 출발 병력군은 서로 합쳐지지 않는 독립 이동 객체다.
- 각 경로의 실제 거리 합계에 따라 도착 시간이 달라진다.
- 따라서 일반적으로 C 병력이 먼저 D에 도착하고, B, A 순서로 도착한다.
- D의 병력은 각 병력군이 도착하는 순간마다 1:1 상쇄 전투 결과로 즉시 갱신된다.

### 유지 규칙
- 21개 Territory / 3팀
- 팀별 시작 Territory 2개
- 50% 출병, 출발지 최소 1병력 잔류
- 점령 직후 약 2.5초 생산 정지
- 이동 병력은 실제 맵 위 오브젝트로 표시
- 13 / 21 Territory 확보 시 승리

---

## 2026-09-05 — v0.1 기준 재정립 / 이동 병력 프로토타입 재구축

사용자가 정리한 `Game 프로젝트 작업 공유본`을 공식 기준으로 재정렬했다. 핵심은 `실제 이동 병력 + 경로/거리 + 순차 도착 전투`다.

초기 구현에서 21개 영토, 3팀, 병력 생산, 점령 후 생산 정지, 50% 출병, 실제 이동 객체, 순차 도착 전투, AI, 13개 점령 승리 등을 구축했다.

---

## 과거 실험 기록
이전 v0.2~v0.3에서 거점형/격자형 Territory 및 다양한 조작 방식을 실험했지만, 현재는 v0.1 계열에서 실제 지도형 Territory와 이동/도착 시스템을 다시 구축한다.