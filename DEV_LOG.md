# TERRITORY GAME 개발 일지

## 2026-09-05 — v0.1.4 마지막 참고 이미지형 전장 재설계

### 방향 변경
사용자가 마지막으로 제공한 전장 참고 이미지의 구조를 1순위 레퍼런스로 채택했다.

기존의 네모난 셀 덩어리형 맵을 버리고, 넓은 전장 안에 충분한 간격을 둔 21개 거점을 배치하고 거점 사이 경로를 시각적으로 읽기 쉬운 구조로 변경했다.

### 맵
- 21개 거점을 화면 전체에 넓게 분산 배치.
- 각 거점은 서로 다른 숨은 타일 수를 가진다.
- 숨은 타일 1개 = `+0.1 / 초` 생산.
- 숨은 타일 수가 많은 거점은 화면의 영역 반경도 더 크게 표시된다.
- 거점 간 이동 가능한 경로는 점선 연결로 표시한다.
- 화면의 칸/격자 느낌을 제거하고 모바일에서 이동 거리와 전선이 한눈에 읽히도록 변경했다.

### 조작 / 이동
- 플레이어 Territory에서 드래그 시작.
- 연결된 아군 거점을 A → B → C 순서로 훑은 뒤 D에 드롭 가능.
- A 병력은 A → B → C → D.
- B 병력은 B → C → D.
- C 병력은 C → D.
- 각 병력군은 독립 이동 객체이며 합산하지 않는다.
- 실제 경로 거리 합계에 따라 각 병력군의 도착 시간이 달라진다.
- 같은 목표에 여러 병력군이 도착하면 도착한 순서대로 전투 결과를 반영한다.
- 아군 Territory에 드롭하면 동일 방식으로 실제 이동 후 병력 보충.

### 유지 규칙
- 3팀: 파랑 플레이어 / 빨강 AI / 초록 AI.
- 팀별 시작 Territory 2개.
- 출병량 50%, 출발지 최소 1병력 유지.
- 점령 직후 약 2.5초 생산 정지.
- 병력 최대 99.
- 13 / 21 Territory 확보 시 승리.

---

## 2026-09-05 — v0.1.3 아군 보충 / 맵 가독성 수정

- 아군 Territory를 목표로 병력을 보낼 수 없던 문제 수정.
- 드래그가 `A → B`에서 끝나고 B가 아군 Territory이면 A의 병력 50%가 B로 실제 이동해 보충.
- `A → B → C`에서 C가 아군이면 A는 A→B→C, B는 B→C 경로로 각각 독립 이동.
- 숨은 타일 1개 = `+0.1 / 초` 생산 규칙 유지.

---

## 2026-09-05 — v0.1.2 보이지 않는 타일 기반 Territory / 드래그 경로 이동

- 각 Territory 내부를 플레이어에게 보이지 않는 작은 타일 집합으로 구성.
- 기본 타일 1개 생산량은 `0.1 / 초`.
- Territory 생산량은 `보유 타일 수 × 0.1 / 초`.
- 드래그 경로에 포함된 각 Territory의 병력이 서로 독립적으로 출발.
- 실제 거리 합계에 따라 도착 시간 차등.
- 목표 전투는 도착 순서대로 1:1 상쇄 처리.

---

## 2026-09-05 — v0.1 기준 재정립 / 이동 병력 프로토타입 재구축

사용자가 정리한 `Game 프로젝트 작업 공유본`을 공식 기준으로 재정렬했다. 핵심은 `실제 이동 병력 + 경로/거리 + 순차 도착 전투`다.

---

## 과거 실험 기록
이전 v0.2~v0.3에서 거점형/격자형 Territory 및 다양한 조작 방식을 실험했지만, 현재는 v0.1 계열에서 실제 이동 병력과 거리 기반 전투를 중심으로 다시 구축한다.