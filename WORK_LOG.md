# TERRITORY GAME — VERSION WORK LOG

이 문서는 버전별로 **무엇을 왜 바꿨는지, 작업 흐름이 어떻게 이어졌는지, 다음 버전에서 무엇을 이어받아야 하는지**를 기록한다.

> 운영 원칙
> - 기능/규칙 변경이 생길 때마다 버전을 올리고 이 문서를 함께 갱신한다.
> - 각 버전은 `요청/문제 → 판단 → 구현 → 유지사항 → 다음 연결` 순서로 기록한다.
> - 이미 확정된 핵심 규칙은 임의로 되돌리지 않는다.

---

## v0.1 — 기준 재정립 / 실제 이동 병력 프로토타입

### 요청 / 문제
초기 실험 버전에서 클릭 즉시 전투나 단순 거점형 구조가 섞이면서 게임의 핵심 감각이 흐려졌다.

### 판단
게임의 정체성을 `실제 이동 병력 + 거리 + 순차 도착 전투`로 다시 고정했다.

### 구현
- 3팀: 파랑 플레이어 / 빨강 AI / 초록 AI
- 총 21 Territory
- 팀별 시작 Territory 2개
- 중립 Territory 15개
- 출병 시 병력 50%, 최소 1병력 잔류
- 병력군은 실제 이동 오브젝트로 존재
- 이동거리/경로에 따라 도착시간 차등
- 도착하는 순서대로 1:1 상쇄 전투
- 점령 직후 약 2.5초 생산 정지
- 13 / 21 Territory 확보 시 즉시 승리

### 다음 연결
맵 크기와 생산량을 같은 기준으로 묶는 구조가 필요해져 숨은 타일 시스템으로 진행.

---

## v0.1.2 — 숨은 생산 타일 + 드래그 연쇄 출병

### 요청 / 문제
Territory 크기가 단순 장식이 아니라 실제 생산력과 연결되어야 했고, 여러 아군 Territory를 훑어 동시에 공격하는 조작이 필요했다.

### 판단
플레이어에게는 보이지 않는 내부 타일을 생산 단위로 사용하고, 드래그 경로에 포함된 각 아군 Territory가 독립 병력군을 보내도록 설계했다.

### 구현
- 숨은 생산 타일 1개 = `+0.1 병력/초`
- Territory 생산량 = `보유 생산 타일 수 × 0.1/초`
- Territory의 시각적 크기도 같은 타일 수에 비례
- 드래그 `A → B → C → D` 시
  - A 병력: A→B→C→D
  - B 병력: B→C→D
  - C 병력: C→D
- 각 병력군은 서로 합쳐지지 않음
- 각자의 실제 경로 길이에 따라 도착시간 독립 계산
- 목표 Territory는 병력군이 도착할 때마다 즉시 전투 결과 갱신

### 유지사항
v0.1의 실제 이동/순차 전투/13개 승리 규칙 유지.

### 다음 연결
공격뿐 아니라 아군 Territory 보충도 같은 이동 원칙으로 처리할 필요가 생김.

---

## v0.1.3 — 아군 보충 + 맵 가독성 수정

### 요청 / 문제
드래그를 아군 Territory에서 끝냈을 때 병력 보충이 되지 않았고, 화면에서 Territory보다 병력 마커와 경계가 너무 강하게 보였다.

### 판단
아군 보충도 공격과 동일하게 실제 이동 객체로 처리하고, UI는 Territory 영역이 더 잘 보이도록 조정.

### 구현
- `A → B`에서 B가 아군이면 A 병력이 실제 이동 후 B에 합류
- `A → B → C`에서 C가 아군이면
  - A: A→B→C
  - B: B→C
- 보충 병력도 거리별 도착시간 유지
- 병력 마커 축소
- 경계선 두께 축소
- 세로 맵 범위 조정

### 다음 연결
사용자가 제공한 마지막 전장 참고 이미지 방향으로 시각 구조를 크게 전환.

---

## v0.1.4 — 전장/거점형 레이아웃 재설계

### 요청 / 문제
기존 지도/격자형 실루엣보다 마지막 참고 이미지처럼 거점 간 거리가 명확하고 이동 동선이 잘 읽히는 전장이 더 적합하다고 판단.

### 판단
실제 국가 지도 형태를 억지로 만들지 않고, 21개 Territory를 넓은 전장에 분산 배치하면서도 각 Territory는 실제 면적과 생산량을 가지도록 유지.

### 구현
- 21개 거점을 전장 전체에 분산
- 거점마다 서로 다른 생산 타일 수
- 생산 타일 수가 많을수록 Territory 표시 영역도 큼
- 인접 가능한 Territory 사이에 이동 연결선 표시
- 실제 이동 병력의 동선이 한눈에 보이도록 구조 변경
- 기존 드래그 연쇄 출병/아군 보충/순차 전투 유지

### 다음 연결
거점 간 직접 거리만 보는 대신, 자동으로 내 영토를 경유하는 최단경로 기능 추가 필요.

---

## v0.1.5 — 내 영토 최단경로 + 거점 간격 확대

### 요청 / 문제
플레이어가 매번 긴 경로를 직접 드래그하지 않고도 내 땅을 경유하는 최단 이동경로를 자동 계산할 수 있어야 했고, 거점 간 거리가 조금 더 멀게 느껴져야 했다.

### 판단
실제 거리 가중치를 쓰는 다익스트라 기반 최단경로를 추가. 수동 드래그는 그대로 유지해 자동/수동을 공존시킴.

### 구현
- 탭 1: 출발 Territory 선택
- 탭 2: 목표 Territory 선택
- 중간 경유지는 현재 플레이어 소유 Territory만 허용
- 마지막 목표는 아군 / 중립 / 적 모두 허용
- 현재는 거점 중심 좌표 사이 실제 거리 합으로 최단경로 계산
- 자동 계산 경로를 잠깐 강조 표시
- 출발 후 경로 고정
- 드래그는 수동 경로 지정으로 계속 사용
- 거점 좌표를 더 벌려 평균 이동거리 확대
- 이동속도를 소폭 낮춰 거리 차이가 도착시간에 더 잘 나타나도록 조정

### 다음 연결 — v0.1.6 예정
이동거리 계산 자체도 좌표 직선거리 대신 **보이지 않는 이동 타일** 기준으로 전환.

---

## v0.1.6 — 예정: 숨은 이동 타일 기반 동선 계산

### 확정 방향
생산과 이동을 서로 다른 숨은 내부 레이어로 분리한다.

- **생산 타일**: Territory 면적 + 생산량 결정
- **이동 타일**: 병력 이동 동선 + 이동거리 + 도착시간 결정
- 두 타일 레이어 모두 플레이어에게 표시하지 않음

### 예정 규칙
- 이동 타일 1칸을 기본 거리 단위로 사용
- 최단경로 탐색은 이동 타일 누적 비용 기준
- 중간 경유 Territory는 자기 팀 소유만 허용
- 마지막 목표만 적/중립/아군 허용
- 병력은 계산된 이동 타일 동선을 따라 실제 이동
- 출발한 병력군은 이후 소유권 변경과 관계없이 출발 시 확정된 경로를 유지
- 여러 병력군은 각자 경로/거리/도착시간을 독립 보유
- 같은 목표라도 도착 순서대로 개별 전투

### 확장 가능성
향후 이동 타일 비용을 다르게 주면 지형 시스템을 바로 얹을 수 있다.
예: 일반 지형 `1.0`, 도로 `0.7`, 산악 `1.5`.

---

## 현재 고정 핵심 규칙

- 3팀 / 21 Territory / 팀당 시작 2 Territory
- 플레이어 파랑, AI 빨강/초록
- 생산 타일 1개 = `+0.1/초`
- Territory 크기와 생산력은 같은 생산 타일 수에 기반
- 출병량 50%, 출발지 최소 1병력 잔류
- 병력은 실제 이동 객체
- 병력군은 합쳐서 즉시 계산하지 않음
- 실제 도착 순서대로 전투
- 점령 직후 생산 정지 약 2.5초
- 병력 최대 약 99
- 13 Territory 확보 시 승리
- 드래그 = 수동 경로
- 탭 선택 = 자동 최단경로

---

## 앞으로의 기록 형식

각 신규 버전은 아래 형식으로 이 문서 최상단 또는 최신 버전 다음에 추가한다.

```text
## vX.X.X — 제목
### 요청 / 문제
### 판단
### 구현
### 유지사항
### 검수 / 발견된 문제
### 다음 연결
```
