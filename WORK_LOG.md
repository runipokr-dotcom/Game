# TERRITORY GAME — VERSION WORK LOG

이 문서는 버전별로 **무엇을 왜 바꿨는지, 작업 흐름이 어떻게 이어졌는지, 다음 버전에서 무엇을 이어받아야 하는지**를 기록한다.

> 운영 원칙: 기능/규칙 변경 시 버전과 로그를 함께 갱신하고, 확정된 실제 이동/순차 전투 핵심은 임의로 되돌리지 않는다.

---

## v0.1.7 — 대형 전장 / 40 Territory / 랜덤 본진

### 요청 / 문제
21개 Territory는 실제 영역 싸움에서 주변 작은 땅을 먹고 더 큰 땅을 노리는 확장감을 만들기엔 전장이 작았다. 플레이어 시작점도 맵 꼭짓점에 고정될 이유가 없으며 이동 체감도 조금 빨라질 필요가 있었다.

### 판단
전장을 크게 늘리고 Territory 수를 약 40개로 확대한다. 시작 위치는 외곽 고정이 아니라 전장 전체에서 랜덤으로 정하되 팀끼리 너무 붙지 않게 한다. 시작 본진은 주변 일반 Territory보다 크게 만들어 초반 확장의 중심으로 사용한다.

### 구현
- 총 Territory: `21 → 40`
- 전장 좌표계: `2200 × 1320`
- 소형 Territory 다수 + 중형/대형 Territory 소수 분포
- 파랑/빨강/초록 시작 위치 매 게임 랜덤
- 각 팀은 서로 인접한 Territory 2개로 시작
- 세 팀 시작쌍 사이 최소 간격을 우선해 배치
- 플레이어 첫 본진 생산 타일 26개 / AI 첫 본진 22개
- 시작점은 꼭짓점/외곽 제한 없음
- 빈 전장 드래그로 화면 이동
- `+ / -` 및 마우스 휠 확대/축소
- 이동속도 `2.6 → 3.2 이동 타일/초`
- 승리조건 `13/21 → 24/40 (60%)`

### 유지사항
- 생산 타일 1개 = +0.1/s
- Territory 면적과 생산력은 같은 생산 타일 수 기반
- 이동은 숨은 이동 타일 기반
- 출병량 50%, 최소 1 잔류
- 병력군은 실제 이동 객체이며 합쳐서 즉시 계산하지 않음
- A→B→C→D 연쇄 출병 시 A/B/C 독립 출병
- 실제 도착 순서대로 1:1 상쇄 전투
- 점령 직후 약 2.5초 생산 정지
- 드래그 수동 경로 / 탭 자동 최단경로

### 검수 / 발견된 문제
- 현재 40개 Territory의 위치/연결망은 대형 전장 체감을 확인하기 위한 1차 레이아웃이다.
- 모바일에서 확대 상태의 배경 드래그와 Territory 출병 드래그는 시작 지점이 빈 공간인지 Territory인지로 구분한다.
- 실제 플레이 후 Territory 밀도, 시작점 간 거리, 3.2 이동속도는 추가 튜닝 가능.

### 다음 연결
대형 전장에서 실제 한 판을 돌려 초반 중립 확장 속도, 큰 Territory의 가치, AI와 최초 충돌 시점, 카메라 조작감을 확인한다. 이후 Territory 모양을 단순 원형에서 더 자연스러운 영역 형태로 발전시키는 것을 검토한다.

---

## v0.1.6 — 숨은 이동 타일 기반 동선 계산
### 요청 / 문제
생산과 이동의 내부 거리 체계를 타일 기준으로 통일할 필요가 있었다.
### 구현
- 생산 타일과 이동 타일 분리
- 이동 타일 약 55px
- 이동속도 당시 2.6칸/초
- 최단경로/도착시간을 이동 타일 비용으로 계산
- 자동/수동 연쇄 출병 및 독립 도착 전투 유지

---

## v0.1.5 — 내 영토 최단경로 + 거점 간격 확대
- 탭 자동 최단경로와 수동 드래그 경로를 공존시킴.

## v0.1.4 — 전장/거점형 레이아웃 재설계
- 마지막 참고 이미지의 넓은 전장 구조를 핵심 시각 방향으로 채택.

## v0.1.3 — 아군 보충 + 맵 가독성 수정
- 아군 보충도 실제 이동 객체로 처리.

## v0.1.2 — 숨은 생산 타일 + 드래그 연쇄 출병
- Territory 크기/생산량을 숨은 생산 타일로 연결하고 다중 독립 출병 구현.

## v0.1 — 기준 재정립 / 실제 이동 병력 프로토타입
- 실제 이동 병력 + 거리 + 순차 도착 전투를 게임 정체성으로 고정.

---

## 현재 고정 핵심 규칙
- 3팀: 파랑 플레이어 / 빨강 AI / 초록 AI
- 40 Territory / 팀당 시작 2개 / 랜덤 시작
- 생산 타일 1개 = +0.1/s
- Territory 크기와 생산력은 같은 생산 타일 수 기반
- 이동거리/최단경로/도착시간은 숨은 이동 타일 기반
- 이동속도 3.2 이동 타일/초
- 출병량 50%, 출발지 최소 1병력 잔류
- 병력은 실제 이동 객체, 실제 도착 순서대로 전투
- 점령 직후 생산 정지 약 2.5초
- 24/40 Territory 확보 시 승리
- 드래그 = 수동 경로 / 탭 = 자동 최단경로